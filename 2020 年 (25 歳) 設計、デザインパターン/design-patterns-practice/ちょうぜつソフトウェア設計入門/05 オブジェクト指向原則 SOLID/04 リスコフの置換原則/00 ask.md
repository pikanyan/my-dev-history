-   次は LSP に関する、「良い感じの説明・表現」を探しています

-   LSP でないコードというのは、ざっくり言えば、やっちゃいけない override をしている

    LSP であるコードというのは、ざっくり言えば、やっちゃいけない override をしていない

    という認識でいいですか

-   良い・悪い「継承」ではなく、良い・悪い「override」の方が説明としてより正確ですか

    悪い override をした結果、利用者側から見て期待しない挙動を起こす、みたいな認識ですか

-   LSP でないコードというのは、悪い override をしている部分であり、

    実践時ではなく、学習時には「悪い override」みたいな感じで可視化できますが

    LSP であるコードというのは、悪い override がないので、可視化できませんよね

    じゃあ、可視化を諦めて、

    コメントで可視化されている LSP でないコードと

    コメントで可視化されていない LSP であるコードを比較してトレーニングするべきですか

-   「悪い override」というコメントはなんだか、抽象的で、説明が悪いです

    どういうコメントが適切ですか

    いくつか候補を挙げてください

-   あなたの説明の

    本質的なポイントは、「Rectangle のインターフェイス (set_width, set_height) や動作ルール」をサブクラスが勝手に変えないこと。

    という部分がいまいちピンと来ないです

    Square クラスの

    ```
    def set_width(self, width: float):
        self._width = width
        self._height = width

    def set_height(self, height: float):
        self._width = height
        self._height = height
    ```

    という部分が、「悪い override」というのは、感覚的には分かりますが

    もっとピンとくる表現を探しています

    利用者側は、set_width をしたつもりが、勝手に set_height を変える「悪い override」をしていることは分かります

-   LSP でないコードも、LSP であるコードも、Square は「幅を変えたら高さも連動する」という挙動をしていますよね

    そして、LSP でないコードは悪い override を行い（コメントでの可視化ができ）

    LSP であるコードは、悪い override を行わない（コメントでの可視化もできない）

    という認識でいいですか
